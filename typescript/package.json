{
  "name": "@ostara-labs/plot",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.12",
    "typescript": "^5.9.0",
    "vitest": "^4.1.11",
    "@types/node": "^26.5.0"
  },
  "version": "0.2.0"
}
